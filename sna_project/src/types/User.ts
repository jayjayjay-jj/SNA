type User = {
    first_name?: string,
    last_name?: string,
    email: string,
    mobile_phone_number?: string,
    password: string,
    role_id?: Number,
    subscribed?: boolean,
    status?: string
}

export default User